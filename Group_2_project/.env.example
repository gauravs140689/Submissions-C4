# ============================================================
# .env.example — Template for API Keys
# ✅ SAFE TO COMMIT — contains no real secrets
# ============================================================
#
# SETUP INSTRUCTIONS:
#   1. Copy this file:  cp .env.example .env
#   2. Fill in your real API keys in .env
#   3. NEVER commit .env to git (it's in .gitignore)
#
# WHERE TO GET KEYS:
#   OPENROUTER_API_KEY  → https://openrouter.ai/keys  (free tier available)
#   TAVILY_API_KEY      → https://tavily.com           (free: 1000 searches/month)
# ============================================================

OPENROUTER_API_KEY=your_openrouter_api_key_here
TAVILY_API_KEY=your_tavily_api_key_here

# Optional: Override the default model (see config.py for options)
# DEFAULT_MODEL=anthropic/claude-3-haiku

# Optional: Set to "true" to enable verbose LLM call logging
# DEBUG_LLM=false
